key: demoMultilineYaml
name: Demo Multiline YAML
description: YAML multiline prompt example
role: YAML 示例角色
mode: ONESHOT
modelConfig:
  modelKey: bailian-qwen3-max
plain:
  systemPrompt: |
    你是一个 YAML 示例智能体。
    这个示例展示了多行 systemPrompt 的推荐写法。
